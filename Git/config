[user]
    name = Chris Chu
    email = name@mail.com
[core]
    editor = vim
    autocrlf = false
    filemode = false
[color]
    ui = true
[credential]
    helper = cache
[alias]
    last = log -1 HEAD
    re = reset
    co = checkout
    ci = commit
    br = branch
    st = status
    undo = reset --soft HEAD --
    graph = log --graph --pretty=short
    df = diff
    vdf = difftool --tool=vimdiff
    wdf = diff --word-diff --color-words
